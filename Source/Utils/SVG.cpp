///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include "Utils/SVG.hpp"

///////////////////////////////////////////////////////////////////////////////
// Namespace UEB
///////////////////////////////////////////////////////////////////////////////
namespace UEB
{

///////////////////////////////////////////////////////////////////////////////
sf::Texture LoadSVG(const FPath& filepath, int width, int height)
{
    auto document = lunasvg::Document::loadFromFile(filepath);
    auto bmap = document->renderToBitmap(width, height);
    sf::Image image(sf::Vector2u(bmap.width(), bmap.height()), bmap.data());
    return (sf::Texture(image));
}

} // !namespace UEB
